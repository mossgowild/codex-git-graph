{
  "mcpServers": {
    "git_graph": {
      "command": "node",
      "args": [
        "-e",
        "const { join } = require('node:path');\nconst root = process.env.CODEX_HOME || join(require('node:os').homedir(), '.codex');\nprocess.argv[1] = join(root, 'plugins/cache/codex-git-graph/git-graph', \"0.2.1+codex.20260919114727\", 'dist/server.mjs');\nimport(require('node:url').pathToFileURL(process.argv[1]).href);"
      ],
      "env_vars": [
        "CODEX_HOME"
      ]
    }
  }
}
