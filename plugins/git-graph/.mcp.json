{
  "mcpServers": {
    "git_graph": {
      "command": "node",
      "args": [
        "-e",
        "const { join } = require('node:path');\nconst root = process.env.CODEX_HOME || join(require('node:os').homedir(), '.codex');\nprocess.argv[1] = join(root, 'plugins/cache/codex-git-graph/git-graph', \"0.1.5+codex.20260915070009\", 'dist/server.mjs');\nimport(require('node:url').pathToFileURL(process.argv[1]).href);"
      ],
      "env_vars": [
        "CODEX_HOME"
      ]
    }
  }
}
